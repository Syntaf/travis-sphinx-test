language: python - "2.7"
sudo: false

install:
    - pip install travis-sphinx
    - export PATH=$HOME/.local/bin:$PATH

script:
    - travis-sphinx --source=test/docs/source --pullrequests --branches=master,test build
    - travis-sphinx deploy
